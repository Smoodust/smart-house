apiVersion: v2
name: smart-house
description: Helm chart for the Smart House application stack
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: ">=0.0.0"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
