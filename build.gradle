plugins {
    id 'java'
}

subprojects {
    apply plugin: 'java'

    group = 'ru.tbank'
    version = '0.5.0'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }

    repositories {
        mavenCentral()
    }
}